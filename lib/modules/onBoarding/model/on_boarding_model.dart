/// as model comes locally it doesn't need serialization...
class OnBoardingObject {
  final String image;
  final String title;
  final String body;

  OnBoardingObject({
    required this.image,
    required this.title,
    required this.body,
  });
}
